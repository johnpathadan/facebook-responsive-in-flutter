//Models that dart is a barrel file where we export all of our different models within our app.
//If we need to import any models, just import models.dart, that has all the models in our app

export 'user_model.dart';
export 'story_model.dart';
export 'post_model.dart';
